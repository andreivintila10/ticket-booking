package ticket.booking.networking;

// BookResponseMsg interface which defines an accessor method for message.
public interface BookResponseMsg extends ResponseMessage {
    public String getMessage();
}
