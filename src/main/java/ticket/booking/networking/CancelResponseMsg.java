package ticket.booking.networking;

// CancelResponseMsg interface which defines an accessor method for message.
public interface CancelResponseMsg extends ResponseMessage {
    public String getMessage();
}
