package ticket.booking.networking;

// ExitResponseMsg interface which defines an accessor method for message.
public interface ExitResponseMsg extends ResponseMessage {
    public String getMessage();
}
